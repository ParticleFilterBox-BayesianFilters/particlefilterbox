# CLI

Command-line interface for particlefilterbox.

## Commands

- `pfbox filter` - Run particle filter
- `pfbox estimate` - Estimate parameters
- `pfbox compare` - Compare models
- `pfbox simulate` - Simulate data
